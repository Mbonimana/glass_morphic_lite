name: example
description: "Alternative Example for glass_morphic_lite package usage"

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  glass_morphic_lite:
    path: ../

flutter:

  uses-material-design: true

